#include <iostream>
using namespace std;

int main(){
int i,b,rem,power=1,temp=0;

cout << "Enter the binary Number : ";
cin >> b;

while(b!= 0){
	rem = b%10;
	b/= 10;
	temp = temp+rem*power;
	power = power*2;
}
cout << temp;
return 0;
}
